{
  "$comment": "AgentNotify harness example for Cursor. Merge into ~/.cursor/hooks.json (or <repo>/.cursor/hooks.json for a project) and replace HOOK_DIR with the folder holding agentnotify_hook.py. Prefer `agentnotify install-harness cursor` so the merge preserves your other hooks. The hooks only notify and never block.",
  "version": 1,
  "hooks": {
    "stop": [
      {
        "command": "python3 HOOK_DIR/agentnotify_hook.py cursor stop"
      }
    ],
    "sessionEnd": [
      {
        "command": "python3 HOOK_DIR/agentnotify_hook.py cursor session-end"
      }
    ]
  }
}
