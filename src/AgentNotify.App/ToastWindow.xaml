<Window x:Class="AgentNotify.App.ToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Focusable="False">
    <Border x:Name="Root"
            Width="380"
            Margin="0"
            CornerRadius="12"
            BorderThickness="1"
            BorderBrush="#27272A"
            Background="#F20E0E11"
            SnapsToDevicePixels="True"
            MouseLeftButtonDown="OnBodyClick"
            ToolTip="Open Notification Center">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="4" Direction="270" Opacity="0.60" Color="Black"/>
        </Border.Effect>
        <Grid Margin="16,14,10,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.RowSpan="3" Grid.Column="0"
                    Width="4" Margin="0,1,12,1" CornerRadius="2"
                    Background="{Binding ElementName=Root, Path=Tag}"/>

            <StackPanel Grid.Column="1" Grid.Row="0" Orientation="Horizontal">
                <TextBlock x:Name="TypeLabel" FontSize="11" FontWeight="SemiBold"
                           Foreground="{Binding ElementName=Root, Path=Tag}"/>
                <TextBlock x:Name="AgentText" Margin="8,0,0,0" FontSize="11"
                           Foreground="#A1A1AA" TextTrimming="CharacterEllipsis"/>
                <TextBlock x:Name="TimeText" Margin="8,0,0,0" FontSize="11"
                           Foreground="#71717A"/>
            </StackPanel>

            <Button x:Name="DismissButton" Grid.Column="2" Grid.Row="0"
                    Width="22" Height="22" Padding="0" Margin="4,-4,-2,0"
                    Background="Transparent" BorderThickness="0"
                    Foreground="#A1A1AA"
                    Click="OnDismissClick">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="11">
                            <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets"
                                       FontSize="11" HorizontalAlignment="Center"
                                       VerticalAlignment="Center" Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FAFAFA"/>
                                <Setter Property="Foreground" Value="#09090B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <TextBlock x:Name="TitleText" Grid.Row="1" Grid.Column="1"
                       Margin="0,3,8,0" FontSize="13" FontWeight="SemiBold"
                       Foreground="#FAFAFA" TextWrapping="Wrap" TextTrimming="CharacterEllipsis"/>

            <TextBlock x:Name="MessageText" Grid.Row="2" Grid.Column="1"
                       Margin="0,2,8,0" FontSize="12"
                       Foreground="#D4D4D8" TextWrapping="Wrap" MaxHeight="52"/>
        </Grid>
    </Border>
</Window>
